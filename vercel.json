{
    "version": 2,
    "builds": [
        { "src": "api/**/*.js", "use": "@vercel/node" },
        { "src": "package.json", "use": "@vercel/static-build" }
    ],
    "functions": {
        "api/**/*.js": {
            "runtime": "nodejs18.x",
            "maxDuration": 10,
            "memory": 1024
        }
    },
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1.js" }
    ]
}